// null is a primitive data type in JavaScript that represents the intentional absence of any object value.
// It is often used to explicitly indicate that a variable should not have a value.

let d = null;

console.log(d); // Output: null

console.log(typeof(d)); // Output: object

// Here, the typeof operator returns "object" for null, which is a known quirk in JavaScript.